<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hansol.hansolproject.mapper.EmployeeMapper">
    <insert id="insert" parameterType="Employee" useGeneratedKeys="true" keyProperty="id">
        insert into employee (name, company1, company2, position, task, telephone)
        values (#{name}, #{company1}, #{company2}, #{position}, #{task}, #{telephone})
    </insert>

</mapper>